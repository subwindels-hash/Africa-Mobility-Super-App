{
  "name": "amsa-backend",
  "version": "1.0.0",
  "description": "Africa Mobility Super App \u2014 NestJS backend monorepo (modular monolith with microservice extraction path)",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/apps/api/main.js",
    "dev": "ts-node-dev --respawn apps/api/main.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint . --ext .ts",
    "typecheck": "tsc --noEmit",
    "test:e2e": "RUN_E2E=1 vitest run tests/e2e",
    "load": "node ../scripts/load/api-load.mjs"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.5.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}